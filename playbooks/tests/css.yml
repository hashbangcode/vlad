---
- name: Test | less is installed
  shell: less --version
  when: css_install is defined and css_install == "y"
  tags: test

- name: Test | sass is installed
  shell: sass -v
  when: css_install is defined and css_install == "y"
  tags: test