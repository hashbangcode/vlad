---
- name: Test | Redis is installed
  shell: redis-cli ping
  when: redis_install is defined and redis_install == "y"
  register: redit_result
  failed_when: "redit_result.rc != 0"
  changed_when: False
  tags: test
