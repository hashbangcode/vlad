---

# These are defaults for Vlad. Do not edit this file as part of your configuration.
#
# This file should only contain default varables for choosing which "components" are to be installed.
# ---------------------------------------------------------------------------------------------------

adminer_install: "y"

apache_install: "y"

imagemagick_install: "n"

mailcatcher_install: "y"

memcached_install: "n"

munin_install: "n"

mysql_install: "y"

node_install: "n"

php_install: "y"

pimpmylog_install: "y"

redis_install: "n"

ruby_install: "y" # Ruby is required by MailCatcher

sendmail_install: "y"

solr_install: "n"

# @TODO: Automate this?
varnish_install: "y" # If you turn this off then make sure you set the http_port to be 80.

xhprof_install: "n"
