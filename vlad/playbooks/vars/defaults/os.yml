---
# OS defaults
apache2_docroot: /var/www/html
# Web docroot is Apache docroot for now
web_docroot: /var/www/html
# Apache default user and group
apache2_user: www-data
apache2_group: www-data
# Same as Apache2 default group
web_group: www-data